# ============================================
# MercaList — Variables de Entorno
# ============================================
# Copia este archivo como .env y ajusta los valores.
#   cp .env.example .env
#
# El backend usa estos defaults si la variable no está definida.
# Solo necesitas cambiar DB_PASS si tu MariaDB tiene contraseña.

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=mercalist_db
DB_USER=root
DB_PASS=Pipesofi2006

# Spring (opcional)
# SERVER_PORT=8080
# SPRING_PROFILES_ACTIVE=dev
